# ===========================================
# LuckinHappy Card Verification System
# Environment Configuration
# ===========================================

# ===========================================
# Server Configuration
# ===========================================
# Server port (change to 3001 for production with reverse proxy)
PORT=3000
# Environment: development | production
NODE_ENV=production

# ===========================================
# Database Configuration
# ===========================================
# SQLite database file path
DB_PATH=./data/luckinhappykami.db

# ===========================================
# JWT Configuration
# ===========================================
# IMPORTANT: Change this to a strong random string in production!
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# JWT token expiration time
JWT_EXPIRES_IN=24h

# ===========================================
# Application Configuration
# ===========================================
# Application name
APP_NAME=LuckinHappy Card Verification System
# Application version
APP_VERSION=1.0.0

# ===========================================
# Upload Configuration
# ===========================================
# Upload directory path
UPLOAD_PATH=./uploads
# Maximum file size in bytes (2MB)
MAX_FILE_SIZE=2097152

# ===========================================
# Security Configuration
# ===========================================
# bcrypt rounds for password hashing (higher = more secure but slower)
BCRYPT_ROUNDS=12
# Rate limiting window in milliseconds (15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum requests per window (set to 0 to disable rate limiting)
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# Production Recommendations
# ===========================================
# 1. Change JWT_SECRET to a long random string
# 2. Set NODE_ENV=production
# 3. Use a reverse proxy (nginx) in production
# 4. Set RATE_LIMIT_MAX_REQUESTS according to your needs
# 5. Consider using environment-specific database paths
# 6. Enable HTTPS in production
