<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="preconnect" href="https://cdn.bootcdn.net">
    <link rel="dns-prefetch" href="//cdn.bootcdn.net">
    <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.0.0/css/all.min.css" media="print" onload="this.media='all'">
    <link rel="icon" href="data:,">
    <style>
        /* 基础样式 */
        :root {
            --primary-color: #3498db;
            --bg-color: #ecf0f3;
            --text-color: #2c3e50;
            --shadow-light: 10px 10px 20px #d1d9e6, -10px -10px 20px #ffffff;
            --glass-bg: rgba(255, 255, 255, 0.25);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            min-height: 100vh;
        }

        /* 导航栏样式 */
        .navbar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            width: 250px;
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--shadow-light);
            z-index: 1000;
            border: 1px solid rgba(255, 255, 255, 0.2);
            cursor: grab;
        }

        .navbar:active {
            cursor: grabbing;
        }

        .nav-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .nav-logo {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 5px;
            text-decoration: none;
            color: var(--text-color);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding-bottom: 15px;
        }

        .nav-logo i {
            font-size: 24px;
            min-width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(45deg, #3498db, #2ecc71);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-logo span {
            font-size: 18px;
            font-weight: 600;
        }

        .nav-links {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .nav-links a {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px 15px;
            color: var(--text-color);
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.3s ease;
            white-space: nowrap;
        }

        .nav-links a:hover,
        .nav-links a.active {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }

        .verify-btn-nav {
            background: linear-gradient(45deg, #3498db, #2ecc71);
            color: white !important;
            border-radius: 25px !important;
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        }

        .verify-btn-nav:hover {
            transform: translateX(5px);
            box-shadow: 0 6px 20px rgba(52, 152, 219, 0.4);
        }

        /* 主内容区域 */
        .main-container {
            margin-left: 100px;
            margin-right: 20px;
            max-width: none;
        }

        /* 英雄区域 */
        .hero-section {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            padding: 60px 40px;
            text-align: center;
            box-shadow: var(--shadow-light);
            margin-bottom: 40px;
        }

        .hero-section h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #3498db, #2ecc71);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-section p {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 30px;
        }

        .hero-actions {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: linear-gradient(45deg, #3498db, #2ecc71);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.2);
            color: var(--text-color);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        /* 统计卡片 */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            box-shadow: var(--shadow-light);
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-card i {
            font-size: 36px;
            margin-bottom: 15px;
            color: var(--primary-color);
        }

        .stat-card h3 {
            margin: 0 0 10px;
            font-size: 18px;
            color: var(--text-color);
        }

        .stat-card .value {
            font-size: 32px;
            font-weight: bold;
            color: var(--text-color);
        }

        /* 特性区域 */
        .features-section {
            margin-bottom: 40px;
        }

        .features-section h2 {
            text-align: center;
            margin-bottom: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            color: var(--text-color);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .feature-card {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 30px;
            display: flex;
            gap: 20px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-light);
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(45deg, #3498db, #2ecc71);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: white;
            flex-shrink: 0;
        }

        .feature-content h3 {
            margin: 0 0 10px;
            color: var(--text-color);
            font-size: 20px;
        }

        .feature-content p {
            margin: 0 0 15px;
            color: #666;
        }

        .feature-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .feature-list li {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 8px;
            color: #666;
        }

        .feature-list li i {
            color: #2ecc71;
            font-size: 12px;
        }

        /* 页脚 */
        .footer {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding: 30px 0;
            text-align: center;
            color: var(--text-color);
            margin-top: 60px;
        }

        /* 响应式设计 */
        @media (max-width: 768px) {
            .navbar {
                position: static;
                transform: none;
                width: 100%;
                margin-bottom: 20px;
                cursor: default;
            }

            .nav-container {
                flex-direction: row;
                justify-content: space-between;
                align-items: center;
            }

            .nav-links {
                display: none;
            }

            .main-container {
                margin-left: 20px;
                margin-right: 20px;
                margin-top: 20px;
            }

            .hero-section {
                padding: 40px 20px;
            }

            .hero-section h1 {
                font-size: 2rem;
            }

            .hero-actions {
                flex-direction: column;
                align-items: center;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .feature-card {
                flex-direction: column;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <!-- 导航栏 -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">
                <i class="fas fa-key"></i>
                <span><%= siteTitle %></span>
            </a>
            <div class="nav-links">
                <a href="/" class="active"><i class="fas fa-home"></i> 首页</a>
                <a href="/verify" class="verify-btn-nav"><i class="fas fa-check-circle"></i> 卡密验证</a>
                <div class="dropdown">
                    <a href="#" class="dropdown-toggle">
                        <i class="fas fa-book"></i> 使用文档 <i class="fas fa-chevron-down"></i>
                    </a>
                </div>
                <a href="#contact"><i class="fas fa-envelope"></i> 联系我们</a>
                <a href="/admin" class="admin-btn"><i class="fas fa-user-shield"></i> 管理登录</a>
            </div>
        </div>
    </nav>

    <!-- 主内容区域 -->
    <div class="main-container">
        <!-- 英雄区域 -->
        <section class="hero-section">
            <h1><%= siteTitle %></h1>
            <p><%= siteSubtitle %></p>
            <div class="hero-actions">
                <a href="/verify" class="btn btn-primary">
                    <i class="fas fa-check-circle"></i> 立即验证
                </a>
                <a href="#features" class="btn btn-secondary">
                    <i class="fas fa-info-circle"></i> 了解更多
                </a>
            </div>
        </section>

        <!-- 统计数据 -->
        <section class="stats-grid">
            <div class="stat-card">
                <i class="fas fa-key"></i>
                <h3>安全可靠</h3>
                <div class="value">99.9%</div>
            </div>
            <div class="stat-card">
                <i class="fas fa-bolt"></i>
                <h3>响应速度</h3>
                <div class="value">< 100ms</div>
            </div>
            <div class="stat-card">
                <i class="fas fa-users"></i>
                <h3>用户满意</h3>
                <div class="value">4.9/5</div>
            </div>
            <div class="stat-card">
                <i class="fas fa-shield-alt"></i>
                <h3>数据安全</h3>
                <div class="value">AES256</div>
            </div>
        </section>

        <!-- 特性介绍 -->
        <section id="features" class="features-section">
            <h2><i class="fas fa-star"></i> 系统特点</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <div class="feature-content">
                        <h3>安全可靠</h3>
                        <p>采用bcrypt密码加密和JWT身份验证，确保数据安全</p>
                        <ul class="feature-list">
                            <li><i class="fas fa-check"></i> bcrypt密码哈希</li>
                            <li><i class="fas fa-check"></i> JWT身份验证</li>
                            <li><i class="fas fa-check"></i> SHA1卡密加密</li>
                            <li><i class="fas fa-check"></i> 设备绑定保护</li>
                        </ul>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-bolt"></i>
                    </div>
                    <div class="feature-content">
                        <h3>高性能</h3>
                        <p>基于Node.js和SQLite的现代化架构，提供卓越性能</p>
                        <ul class="feature-list">
                            <li><i class="fas fa-check"></i> TypeScript类型安全</li>
                            <li><i class="fas fa-check"></i> 异步处理优化</li>
                            <li><i class="fas fa-check"></i> 智能数据库索引</li>
                            <li><i class="fas fa-check"></i> 内存使用优化</li>
                        </ul>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-code"></i>
                    </div>
                    <div class="feature-content">
                        <h3>API友好</h3>
                        <p>提供完整的RESTful API，支持多种验证方式</p>
                        <ul class="feature-list">
                            <li><i class="fas fa-check"></i> RESTful API设计</li>
                            <li><i class="fas fa-check"></i> 多API密钥支持</li>
                            <li><i class="fas fa-check"></i> POST/GET双重支持</li>
                            <li><i class="fas fa-check"></i> 详细使用文档</li>
                        </ul>
                    </div>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <div class="feature-content">
                        <h3>数据统计</h3>
                        <p>完整的数据统计和分析功能，直观展示使用情况</p>
                        <ul class="feature-list">
                            <li><i class="fas fa-check"></i> 实时统计数据</li>
                            <li><i class="fas fa-check"></i> 可视化图表</li>
                            <li><i class="fas fa-check"></i> API调用统计</li>
                            <li><i class="fas fa-check"></i> 使用趋势分析</li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>
    </div>

    <!-- 页脚 -->
    <footer class="footer">
        <div class="container">
            &copy; <%= new Date().getFullYear() %> <%= siteTitle %> - All Rights Reserved
        </div>
    </footer>

    <!-- JavaScript延迟加载 -->
    <script>
        // 延迟加载非关键JavaScript
        function loadDeferredScripts() {
            // 导航栏拖拽功能
            const navbar = document.querySelector('.navbar');
            if (navbar && window.innerWidth > 768) {
                let isDragging = false;
                let startX, startY, initialX, initialY;

                navbar.addEventListener('mousedown', (e) => {
                    isDragging = true;
                    startX = e.clientX;
                    startY = e.clientY;
                    const rect = navbar.getBoundingClientRect();
                    initialX = rect.left;
                    initialY = rect.top;
                    navbar.style.cursor = 'grabbing';
                });

                document.addEventListener('mousemove', (e) => {
                    if (!isDragging) return;
                    const dx = e.clientX - startX;
                    const dy = e.clientY - startY;
                    navbar.style.left = `${initialX + dx}px`;
                    navbar.style.top = `${initialY + dy}px`;
                    navbar.style.transform = 'none';
                });

                document.addEventListener('mouseup', () => {
                    isDragging = false;
                    navbar.style.cursor = 'grab';
                });
            }

            // 平滑滚动
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth'
                        });
                    }
                });
            });
        }

        // 页面加载完成后延迟执行
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', () => {
                setTimeout(loadDeferredScripts, 100);
            });
        } else {
            setTimeout(loadDeferredScripts, 100);
        }
    </script>
</body>
</html>
